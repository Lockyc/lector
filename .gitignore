# Ephemeral spec/plan/brainstorm scratch — kept on disk, never tracked
docs/superpowers/
.superpowers/

# Agent worktrees — a checkout of this repo's own branches; committing it is circular
.claude/worktrees/

# Generated at build time from the pinned chrome-core crate (build.rs); reproducible, not tracked
/src/chrome-core.css
/src/chrome-core.js

# Generated at build time from the pinned shell-core crate (build.rs); reproducible, not tracked.
# The tracked scripts/tooling.env supplies the per-app params. Edit these in shell-core, not here.
/scripts/release.sh
/scripts/gen-latest-json.sh
/scripts/install-app.sh
/scripts/launch-app.sh

# macOS
.DS_Store

# Rust / Tauri build output
/target
/src-tauri/target

# Generated by tauri-build
/src-tauri/gen/schemas

# Generated updater manifest (built by scripts/gen-latest-json.sh for a release upload)
/latest.json

# Release zips built by scripts/release.sh for upload — the release carries them, not the tree
/lector-*-macos.zip
