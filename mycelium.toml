name    = "lector"
summary = "Curated rendered docs — a macOS console of grouped tabs, each a local Markdown repo rendered live and reloading as you edit."
kind    = "app"
status  = "active"
tags    = ["docs", "tauri", "macos", "markdown", "live-reload"]
stack   = ["rust", "tauri", "javascript"]

[[provides]]
name    = "local-doc-console"
summary = "Renders doc repos straight off the working tree — one compositor serve loop per live tab on an ephemeral loopback port, watching for edits. What curator structurally cannot do: no deploy, nothing published."
